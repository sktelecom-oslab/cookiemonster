kind: Service
apiVersion: v1
metadata:
  name: cookiemonster
spec:
  selector:
    app: cookiemonster
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort
